---
import { faqItems } from '@data/faq';
---

<section id="faq" class="section bg-white">
  <div class="container-wide px-4">
    <div class="text-center max-w-3xl mx-auto mb-12 md:mb-16">
      <span class="tag-label bg-rn-yellow/20 text-amber-700 mb-4">
        Perguntas Frequentes
      </span>
      <h2 class="section-title text-balance">
        Perguntas e Respostas
      </h2>
      <p class="section-subtitle mx-auto mb-0">
        Respostas rápidas sobre as propostas apresentadas e informações institucionais.
      </p>
    </div>

    <div class="max-w-3xl mx-auto space-y-3">
      {
        faqItems.map((item, idx) => (
          <div
            data-faq-item
            class="border border-slate-200 rounded-2xl bg-white overflow-hidden"
          >
            <button
              type="button"
              data-faq-trigger
              aria-expanded={idx === 0 ? 'true' : 'false'}
              aria-controls={`faq-panel-${idx}`}
              class="w-full flex items-start justify-between gap-4 text-left p-5 md:p-6 hover:bg-slate-50 transition-colors"
            >
              <span class="font-display font-semibold text-base md:text-lg text-slate-900">
                {item.pergunta}
              </span>
              <span
                data-faq-icon
                class={`mt-1 flex-shrink-0 w-8 h-8 rounded-full flex items-center justify-center transition-all duration-200 ${
                  idx === 0 ? 'bg-rn-blue text-white rotate-180' : 'bg-slate-100 text-slate-600'
                }`}
                aria-hidden="true"
              >
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" class="w-4 h-4">
                  <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
                </svg>
              </span>
            </button>
            <div
              id={`faq-panel-${idx}`}
              role="region"
              data-faq-panel
              class={`grid transition-all duration-300 ease-out ${
                idx === 0 ? 'grid-rows-[1fr]' : 'grid-rows-[0fr]'
              }`}
            >
              <div class="overflow-hidden">
                <div class="px-5 md:px-6 pb-5 md:pb-6 pt-0 text-slate-600 leading-relaxed text-sm md:text-base border-t border-slate-100">
                  <p class="pt-4">{item.resposta}</p>
                </div>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<script>
  /// <reference lib="dom" />
  /// <reference lib="es2015" />
  document.querySelectorAll('[data-faq-item]').forEach((item) => {
    const trigger = item.querySelector('[data-faq-trigger]') as HTMLButtonElement | null;
    const panel = item.querySelector('[data-faq-panel]') as HTMLElement | null;
    const icon = item.querySelector('[data-faq-icon]') as HTMLElement | null;
    if (!trigger || !panel || !icon) return;

    trigger.addEventListener('click', () => {
      const expanded = trigger.getAttribute('aria-expanded') === 'true';
      const willOpen = !expanded;

      trigger.setAttribute('aria-expanded', String(willOpen));
      panel.classList.toggle('grid-rows-[1fr]', willOpen);
      panel.classList.toggle('grid-rows-[0fr]', !willOpen);
      icon.classList.toggle('bg-rn-blue', willOpen);
      icon.classList.toggle('text-white', willOpen);
      icon.classList.toggle('rotate-180', willOpen);
      icon.classList.toggle('bg-slate-100', !willOpen);
      icon.classList.toggle('text-slate-600', !willOpen);
    });
  });
</script>
